<h1>Edit Category</h1>
<%= form_with(model: @category, url: edit_category_path(@category), method: :get, local: true) do |f| %>
 
  <% if @category.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@user.errors.count, "error") %> prohibited
        this user from being saved:
      </h2>
      <ul>
        <% @user.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :category_name %><br>
    <%= f.text_field :category_name %>
  </p>
    <p>
    <%= f.label :category_description %><br>
    <%= f.text_field :category_description %>
  </p>
  <p>
    <div class="actions">
    <%= f.submit "edit" %>
    </div>
  </p>
<% end %>
<%= link_to 'Back', categories_path, method: :get %>