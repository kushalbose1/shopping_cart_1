<h1>Products</h1>
<table class="table">
  <thead>
    <tr>
      <th>Product name</th>
      
      <th>Product price</th>
      
      <th>Quantity</th>
      <th>Edit</th>
      <th>Show</th>
      <th>Delete</th>
      
    </tr>
  </thead>
  <tbody>
    <% @products.each do |product| %>
      <tr>
        <td><%= product.product_name %></td>
        
        <td><%= product.product_price %></td>
        <td><%= product.quantity %></td>
        <td><%= link_to'Show',product_path(product.id), method: :get %></td>
        <td><%= link_to'Edit',edit_product_path(product.id), method: :get %></td>
        <td><%= link_to'Destroy',product, method: :delete %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>
<%= button_to 'New Product', new_product_path, method: :get %>
